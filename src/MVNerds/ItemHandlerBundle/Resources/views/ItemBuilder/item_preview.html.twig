<div class="row-fluid preview-header {{ app.request.getLocale() }}" data-dissmiss="{{ item.getSlug() }}">
	<div class="span6">
		<h3>{{ item.getName() }}</h3>
	</div>
	<div class="span6">
		<p>
			 {{ item.getCost() }} gold
		</p>
	</div>
</div>

<div class="row-fluid">
	<div class="span6">
		
	</div>
	<div class="span6 right-side">
		<div class="item-portrait {{ item.getRiotCode() }}">
			<a href="#" class="btn-action-item-preview"><strong>En savoir plus</strong> sur {{ item.getName() }}</a>
			<a href="#" class="btn-action-item-preview btn-add-to-list" data-item-slug="{{ item.getSlug() }}"><strong>Ajouter</strong> à la liste</a>
		</div>
	</div>
</div>